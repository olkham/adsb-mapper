{
  "name": "adsb-mapper",
  "version": "1.0.0",
  "description": "Local FlightRadar24-style map that plots ADS-B aircraft from an MQTT broker (adsb2mqtt schema).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5188 --strictPort"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "mqtt": "^5.10.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
